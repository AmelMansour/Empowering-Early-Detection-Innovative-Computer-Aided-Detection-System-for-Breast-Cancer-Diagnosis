image:
  file: .gitpod.Dockerfile

tasks:
  - command: |
      echo "Welcome to your Gitpod workspace!"
      pip install -r requirements.txt
      # Ajoute d'autres commandes pour configurer ton environnement ici

ports:
  - port: 8888
    onOpen: open-preview
